services:
  fuzz:
    build:
      context: ..
      dockerfile: fuzz/dockerfile
    volumes:
      - ..:/workspace
    working_dir: /workspace/fuzz
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    environment:
      HOME: /tmp
    command: ["./run.sh"]
